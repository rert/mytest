git config --global user.name "user_name"

git config --global user.email "email_id"

git init Mytest 

cd Mytest

gedit README

//Build the index to be pushed
git add README
git add hello.cpp

git commit -m "message"

git remote add origin https://github.com/rert/mytest.git

git push origin master